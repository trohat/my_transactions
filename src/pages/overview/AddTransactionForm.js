import React from "react";
import { formatDate } from "../../util/helpers";

class AddTransactionForm extends React.Component {
  render() {
    return <form action="">formatDate(1);</form>;
  }
}

export default AddTransactionForm;
